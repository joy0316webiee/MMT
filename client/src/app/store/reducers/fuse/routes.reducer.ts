// import routesConfig from 'app/fuse-configs/routesConfig';

const initialState = {};

const routes = (state = initialState, action: any) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default routes;
